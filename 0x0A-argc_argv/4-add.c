#include <stdio.h>

/**
 * main - adds the positive numbers
 * @argc: 
 * @argv:
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int i, j;
	int sum = 0;

	if (argc == 1)
		printf("%d\n", 0);
	else
	{
		
	}
}
